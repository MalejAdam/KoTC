import { styled, Typography } from '@mui/material';
import { lightRed } from './LoadDistribution.utils';

export const ErrorText = styled(Typography)({
    color: lightRed,
    textAlign: 'center'
})
